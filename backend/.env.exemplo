# Copie para .env e preencha antes de rodar o backend.

# Gere com: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
FERNET_KEY=

DATABASE_URL=sqlite:///./visconde.db
SESSION_TTL_DIAS=30
